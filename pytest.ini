[pytest]
testpaths = tests
markers =
    e2e: multi-process end-to-end tests (spawn real peer workers on localhost)
    integration: in-process integration tests across distribution and security
addopts = -ra
